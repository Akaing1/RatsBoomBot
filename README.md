# Twitch Chat Bot

A modular Twitch chatbot built with TwitchIO.

## Features

- EventSub
- Modular commands
- SQLite token storage
- OAuth authorization
- Extensible architecture

## Setup

1. Clone the repository
2. Create a `.env` from `.env.example`
3. Install dependencies:

```bash
pip install -r requirements.txt