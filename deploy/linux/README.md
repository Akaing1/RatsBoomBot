# RatsBoomBot Linux Deployment

This directory contains deployment files for running RatsBoomBot as a Linux service.

## Files

- `ratsboombot.service` — systemd service definition
- `backup.sh` — SQLite backup script

## Planned install path


```text
/opt/ratsboombot
```